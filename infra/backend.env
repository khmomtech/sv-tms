# Backend environment (edit values, do NOT commit real secrets)
SPRING_PROFILES_ACTIVE=dev
DATABASE_URL=jdbc:mysql://mysql:3306/svlogistics_tms_db
DATABASE_USERNAME=svtms
DATABASE_PASSWORD=svtms
JWT_SECRET=change_me
FIREBASE_CREDENTIALS_PATH=/secrets/firebase.json
UPLOADS_DIR=/app/uploads

# Optional cloud provider settings
# S3_BUCKET=your-bucket
# S3_REGION=us-east-1

# Temporarily disable Flyway for quick startup testing (remove when DB migrations are fixed)
SPRING_FLYWAY_ENABLED=false
