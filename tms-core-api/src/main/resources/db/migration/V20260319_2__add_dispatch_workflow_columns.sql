ALTER TABLE dispatches
  ADD COLUMN IF NOT EXISTS loading_type_code VARCHAR(30) NOT NULL DEFAULT 'GENERAL',
  ADD COLUMN IF NOT EXISTS workflow_version_id BIGINT NULL,
  ADD COLUMN IF NOT EXISTS approval_status VARCHAR(50) NULL,
  ADD COLUMN IF NOT EXISTS approved_by BIGINT NULL,
  ADD COLUMN IF NOT EXISTS approved_at DATETIME(6) NULL,
  ADD COLUMN IF NOT EXISTS approval_remarks TEXT NULL,
  ADD COLUMN IF NOT EXISTS pol_required BIT(1) NOT NULL DEFAULT b'0',
  ADD COLUMN IF NOT EXISTS pol_submitted BIT(1) NOT NULL DEFAULT b'0',
  ADD COLUMN IF NOT EXISTS pol_submitted_at DATETIME(6) NULL,
  ADD COLUMN IF NOT EXISTS pod_required BIT(1) NOT NULL DEFAULT b'0',
  ADD COLUMN IF NOT EXISTS pod_submitted BIT(1) NOT NULL DEFAULT b'0',
  ADD COLUMN IF NOT EXISTS pod_submitted_at DATETIME(6) NULL,
  ADD COLUMN IF NOT EXISTS pod_verified BIT(1) NOT NULL DEFAULT b'0',
  ADD COLUMN IF NOT EXISTS pre_entry_safety_status VARCHAR(50) NULL,
  ADD COLUMN IF NOT EXISTS pre_entry_safety_required BIT(1) NOT NULL DEFAULT b'0';
