global:
  resolve_timeout: 5m

route:
  receiver: 'slack-notifications'

receivers:
  - name: 'slack-notifications'
    # configure your webhook in place of the URL below
    webhook_configs:
      - url: 'https://hooks.example.com/services/XXX/YYY/ZZZ'
