import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-driver-app-devices',
  template: `<h1>Driver App Devices</h1>`,
  standalone: true,
  imports: [RouterModule],
})
export class DriverAppDevicesComponent {}
