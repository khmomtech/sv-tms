import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-driver-shifts',
  template: `<h1>Driver Shifts</h1>`,
  standalone: true,
  imports: [RouterModule],
})
export class DriverShiftsComponent {}
