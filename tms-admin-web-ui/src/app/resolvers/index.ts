// Barrel export for all resolvers
// This allows importing multiple resolvers from a single source: import { DriverDocumentsResolver } from '@app/resolvers';

export * from './driver-documents.resolver';
