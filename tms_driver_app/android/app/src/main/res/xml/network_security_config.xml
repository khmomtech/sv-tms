<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration
  - Blocks cleartext HTTP for all domains (production-safe)
  - Allows cleartext only to localhost for local dev builds
  - Pins or trusts system CAs for all production domains
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only on localhost for dev/emulator testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
