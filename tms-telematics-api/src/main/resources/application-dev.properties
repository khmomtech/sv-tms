# Dev profile overrides — safe defaults for local development
spring.datasource.url=jdbc:postgresql://localhost:5432/svlogistics_telematics
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

location.mongo.enabled=false

logging.level.com.svtrucking.telematics=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.messaging=DEBUG
