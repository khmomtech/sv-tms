version: '3.8'

services:
  tms-safety-api:
    build: .
    container_name: tms-safety-api
    ports:
      - "8081:8080"
    volumes:
      - /opt/tms-safety-api/application.properties:/app/application.properties
    environment:
      - SPRING_CONFIG_LOCATION=file:/app/application.properties
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl --fail http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
